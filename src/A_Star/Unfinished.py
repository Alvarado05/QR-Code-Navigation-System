def calculateCoordinates(nodes, connections):
    startNode = connections.iloc[0,0]
    df = nodes
    # df['X'] = 
    print([None for i in range(df.size)])
    # df['Y'] = [None for i in range(df.size)]
    print(df)